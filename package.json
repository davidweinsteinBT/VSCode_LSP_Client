{
  "name": "vscode_client",
  "version": "1.0.0",
  "description": "",
  "main": "./out/extension",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "eslint": "^8.7.0",
    "typescript": "^4.5.5"
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -b",
    "watch": "tsc -b -w",
    "lint": "eslint ./src --ext .ts,.tsx"
  }
}
